TODO: lesson not written. Below is the planned outline: one line per instructional item, stated as its goal, with the expected format in brackets.
Prior knowledge assumed everywhere in klijent/: HTML, CSS, JavaScript (ES2015+, async/await, fetch), React basics (function components, props, useState), the server segments of this knowledge base.

Scope: one static component: class, template, bindings. Ends on the failure that motivates signals.

Outline:
1. Anchor: a React function component returning JSX beside the same component in Angular as a class with a separate template [code side by side + text]
2. Definition of the component's three files and the decorator that ties them; the selector as a custom HTML tag [code + text]
3. Interpolation {{ }} to show a class member; why members read by the template are protected [code + text]
4. Property binding [value], [disabled]: an attribute driven by class state [code + text]
5. Event binding (click) calling a method; $event as the DOM event [code + text]
6. Integration: a static card component with a name, a description and a disabled button, which is the project's tour card without its input [code + analysis]
7. Closing failure: the click handler assigns to a plain class field and the screen does not change; named as the next lesson's subject, not explained [code + text]

Out of scope: signals, control flow, inputs and outputs, forms, two-way binding, pipes, NgClass and NgStyle.
