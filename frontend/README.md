# Explorer frontend

Angular application for the Explorer platform.

## Running

```bash
npm install
npm start
```

The app runs on `http://localhost:4200/`. Requests to `/api` are proxied to the
backend, which is expected on `http://localhost:5000`.
